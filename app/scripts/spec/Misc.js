/*global define, describe, it*/

define([], function () {
	'use strict';

	// Just testing out that the Require.js stuff is setup correctly
	describe('RequireJS', function () {
        it('is setup for the tests', function () {
            return;
        });
    });
});